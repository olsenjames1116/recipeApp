function AccountFormContainer({ children }: { children: React.ReactNode }) {
	return (
		<div>
			<img src="" alt="" />
			<span>App Title</span>
			<span>Slogan</span>
			{children}
		</div>
	);
}

export default AccountFormContainer;
